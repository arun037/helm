apiVersion: v1
kind: Service
metadata:
    name: {{ .Release.Name }}-svc
    labels:
        app: {{ .Chart.Name }}
spec:
    selector:
        app: {{ .Chart.Name }}
    type: {{ .Values.service }}
    ports:
    - port: 80
      targetPort: 80